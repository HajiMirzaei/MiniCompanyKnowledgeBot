# Project Memory
### At first I created the project brief and tasks and then asked claude to review these files and find any gaps or anything that I didn't think about it.
### Then I started doing the tasks one by one and meanwhile I checked the context and usage and I used compact command to optimize the context after implementing the console app. Before that it was not necessary.
### I had to update the tasks during implementaion becuase I encounterd issues that needed to update the tasks.
### Changed all the implementation in async way, at first they implenmented in sync.
### Meged two tasks (implementing console app and appsettings.json) in order to managing context efficiently.
### It was better to add unit test for each task if possible (my mistake).